/**
 * ป้ายบอกว่านี่คือรุ่นทดลอง
 *
 * ทำไมต้องมี: ลิงก์สาธารณะ (GitHub Pages / ลิงก์เดโม) เอาไว้ให้เพื่อนและอาจารย์กดเล่น
 * ข้อมูลทุกอย่างเป็นของสมมติและเก็บอยู่ในเครื่องคนเปิดเท่านั้น
 * ถ้าไม่บอกไว้ คนจะเข้าใจว่านี่คือระบบจริงแล้วสงสัยว่าทำไมงานที่กรอกไปหายไป
 *
 * แถบเดโมด้านบนบอกอยู่แล้ว แต่มันซ่อนบนจอมือถือ ซึ่งเป็นเครื่องที่คนส่วนใหญ่เปิด
 * ป้ายนี้จึงขึ้นทุกขนาดจอ และขึ้นเฉพาะตอนที่ไม่ได้ต่อเซิร์ฟเวอร์จริง
 */
import { cloudEnabled } from '../lib/cloud';
import { t } from '../lib/i18n';

export function BetaBadge({ compact = false }: { compact?: boolean }) {
  if (cloudEnabled) return null;
  return (
    <span
      className="betabadge"
      title={t('รุ่นทดลอง — ข้อมูลเป็นของสมมติ และเก็บอยู่ในเครื่องนี้เท่านั้น')}
    >
      BETA{compact ? '' : ` · ${t('ข้อมูลสมมติ')}`}
    </span>
  );
}
