-- ═══════════════════════════════════════════════════════════════════════════
-- 0022 · ถอดสิทธิ์หัวหน้าภาคจากบัญชีสาธิต (14 ก.ย. 69)
--
-- ปัญหา (ตรวจ 13 ก.ย.): 0005 ตั้ง demo@example.com เป็นหัวหน้าภาค
--   ใครได้บัญชีนี้ = เห็นข้อมูลทั้งระบบ จัดการรายชื่อเชิญ และสั่งลบข้อมูลได้
--   และถ้าหน้า Authentication ปิด "Confirm email" ใครก็สมัครด้วยอีเมลนี้ได้
--
-- ผู้ใช้เคาะ: demo@example.com ยังเก็บไว้พรีเซนต์ (สลับ นศ.↔อาจารย์ ได้เหมือนเดิม)
--   แต่ไม่ใช่หัวหน้าภาคแล้ว · nak-test@ / ajarn-test@ ไม่แตะ
--
-- ⚠️ สิทธิ์หัวหน้าภาคย้ายไปที่บัญชีเจ้าของระบบ — ตั้ง "ก่อน" รันไฟล์นี้
--    ด้วย SQL ที่ใส่อีเมลจริง ซึ่งจงใจไม่เก็บใน repo (repo เป็น public)
--    ถ้ารันไฟล์นี้อย่างเดียว ระบบจะไม่มีหัวหน้าภาคเลย → จัดการรายชื่อในแอปไม่ได้
--    (ยังแก้ผ่าน SQL Editor ได้เสมอ ไม่มีอะไรหาย)
--
-- ไม่ลบข้อมูล ไม่ลบบัญชี · รันซ้ำได้
-- วิธีติดตั้ง: SQL Editor → Run · แล้วรัน supabase/security-check.sql ดูแถว ⑧ ⑨
-- ═══════════════════════════════════════════════════════════════════════════

update invites   set is_admin = false where lower(email) = 'demo@example.com';
update app_users set is_admin = false where lower(email) = 'demo@example.com';
