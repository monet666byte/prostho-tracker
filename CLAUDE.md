# คู่มือสำหรับ Claude — Prostho Tracker

อ่านไฟล์นี้ก่อนแตะโค้ด — ไฟล์นี้บอกแค่ "ต้องรู้อะไรก่อนลงมือ" กับ "กับดักที่เคยทำให้พังมาแล้ว"
รายละเอียดระบบอยู่ใน `README.md` · ตั้งเครื่อง/รัน/ปล่อยรุ่น อยู่ใน `docs/dev-setup.md`
**สถานะเซิร์ฟเวอร์ · migration ที่รันแล้ว · งานค้าง · เรื่องที่รอตัดสิน อยู่ใน `docs/status.md`** — ไฟล์นี้ไม่เก็บสถานะ

## นี่คืออะไร

PWA ติดตามความคืบหน้าเคสทันตกรรมประดิษฐ์ วิชา DTPT502 ภาควิชาทันตกรรมประดิษฐ์ ม.มหิดล
ใช้แทนการกรอก Google Sheets เดิม · React + TypeScript + Vite + Dexie(IndexedDB) + Supabase
สองฝั่ง: **นักศึกษา** (มือถือ ออฟไลน์ได้) และ **อาจารย์** (จอใหญ่ dashboard ทั้งชั้นปี)

ผู้ใช้ = เจ้าของ/ผู้ดูแลโปรเจกต์ เป็นทันตแพทย์ ไม่ใช่โปรแกรมเมอร์อาชีพ — **อธิบายเป็นภาษาคน** บอกผลกระทบก่อนบอกชื่อฟังก์ชัน · คุยภาษาไทย

## กฎที่ห้ามฝ่าฝืน

1. **repo = ข้อมูลสมมติเสมอ · เซิร์ฟเวอร์นำร่องอาจมีข้อมูลจริง** — ภาค/คณะอนุญาตให้เก็บข้อมูลผู้ป่วยจริงช่วงนำร่อง
   (เว็บนำร่อง https://prostho-tracker.vercel.app) ห้ามเขียนว่า repo มีข้อมูลจริง และห้ามเขียนว่าเซิร์ฟเวอร์ "ไม่มีข้อมูลจริง"
   · สคริปต์ทุกตัวที่ต่อเซิร์ฟเวอร์ต้องคิดว่ากำลังแตะข้อมูลคนไข้จริง
2. **ห้ามใส่ชื่อหรือ HN ผู้ป่วยลงในข้อความ audit log** — แถว audit ลบไม่ได้ตามการออกแบบ ใส่แล้วลบตาม retention ไม่ได้ ใช้รหัสเคสจาก `lib/privacy.ts` แทน
3. **สวิตช์ PDPA ทุกตัวค่าเริ่มต้นคือ "ปิด" = ค่าที่เข้มที่สุด** (`pdpa_policy` · `0016` · `0026`) — `mask_by_default` จึงเป็น `true`
   และ `patient_names` เป็น `false` · ห้ามเปลี่ยนค่าเริ่มต้นหรือลดความเข้มของ trigger ใน `0009`/`0016`/`0026` — คณะยังไม่อนุมัตินโยบายฉบับจริง
4. **ห้ามแก้ migration ที่รันไปแล้ว** ต้องแก้ = เขียนไฟล์ใหม่เลขถัดไป · ⚠️ **ห้ามเดาว่าไฟล์ไหนรันแล้ว** — โปรเจกต์รัน SQL ด้วยมือ
   ไม่มีตารางบันทึกเวอร์ชัน ให้ผู้ใช้รัน `supabase/check-migrations.sql` แล้วอ่านผล (statement เดียว จึงไม่ถูกกลบเหมือน `audit.sql`)
   เคยเขียนสถานะผิดในไฟล์นี้มาสองรอบเพราะเชื่อความจำ · สถานะที่ยืนยันล่าสุดอยู่ใน `docs/status.md`
5. **ห้าม `git add -A`** — ผู้ใช้เปิดหลายเซสชันพร้อมกันได้ ให้ `git add` เฉพาะไฟล์ที่ตัวเองแก้ · เช็ค `git status` ก่อนเสมอ
   ถ้ามีไฟล์แปลกหน้าโผล่มาระหว่างทำงาน = มีเซสชันอื่นอยู่ ให้บอกผู้ใช้
6. **commit ในเครื่องได้ แต่ห้าม push** — ผู้ใช้กด Push origin เองใน GitHub Desktop · ข้อความ commit เป็นภาษาไทย เล่าว่า *ผู้ใช้เจออะไร* ไม่ใช่ *แก้ฟังก์ชันไหน*
7. **ห้ามพูดเรื่องเกรด** ในทุกข้อความที่อาจารย์เห็น — ระบบนี้ไม่ได้ตัดเกรด และ**ห้ามเสนอ Payment เป็นฟีเจอร์**
   ⚠️ แอปยังมีช่อง `payment` ในชิ้นงานและคอลัมน์ `Payment` ใน CSV ติดมาจากชีตเดิม (`domain/types.ts` · `lib/export.ts`)
   เจ้าของต้องตัดสินว่าเก็บหรือถอด (`docs/status.md`) — ห้ามต่อยอดหรือทำหน้าจอให้มัน

## ตัวเลขต้องตรงกันทุกหน้า

นี่คือหัวใจของโปรเจกต์ บั๊กที่แพงที่สุดไม่ใช่จอขาว แต่คือ **หน้าอาจารย์ขึ้นเลขสวย ๆ ที่ผิด**
แล้วอาจารย์เอาไปตัดสินใจว่าจะไปตามนักศึกษาคนไหน

- **กติกาทั้งหมดอยู่ใน `src/domain/` เท่านั้น** — ห้ามคำนวณเกณฑ์/สถานะ/เปอร์เซ็นต์ในไฟล์ `routes/`
  ต้องนับอะไรใหม่ ให้เพิ่มฟังก์ชันใน `domain/` แล้วให้หน้าจอเรียก ไม่งั้นอีกหน้าจะนับคนละแบบ · ตัวช่วยที่มีแล้ว ห้ามเขียนซ้ำ:
  แถบความคืบหน้า `stepFraction(w)` · ขั้นที่ผ่าน `stepsPassed(w)` · "ประเมิน Section II/III แล้วหรือยัง" `isSect2Evaluated` /
  `isSect3Evaluated` · คำตอบแบบประเมินตนเอง `readableAnswer` · ปีที่หน้าอาจารย์เปิดมา `defaultYearView` (`hooks/useYearView.ts`)
- **`domain/catalog.ts` คือ source of truth** ของรายการ procedure ห้าม hard-code ชื่อ step หรือจำนวนขั้น
  ประเภทงาน "นับต่อ arch / ถอดได้ / ต่อซี่" ใช้ `isArchWork` (CD·RPD·APD) · `isRemovableType` (+RRM · Section II) ·
  `isToothWork` (PC·CB·RFX) — ห้ามเขียน `type === 'CD' || …` เอง (เคยมี 5 สำเนาแล้วไม่ตรงกัน)
- **ห้าม hard-code เลข 10** เป็นขั้นสุดท้าย — งาน Recall จบที่ขั้น **3** ใช้ `maxProgression(w)` เสมอ
- **เกณฑ์มีสองชุด ห้ามสลับกัน** (`domain/catalog.ts`) `CUM_REQ_TYPES` = สะสม (CD · RPD · PC · CB · **RRM · RFX**) ·
  `REQ_TYPES` = รายปี (4 ตัวแรก) · Recall เข้าสะสมแต่ไม่เข้ารายปี — `countsTowardRequirement` อ่านชุดสะสม
  ส่วน `completedInYear` / burn-up / ตารางเสี่ยง อ่านชุดรายปีผ่าน **`countsForYearlyReq` (`domain/rules.ts`) ตัวเดียว**
- **เพิ่มช่องใน `Settings.req` ต้องทำสามที่พร้อมกัน** — `DEFAULT_SETTINGS`, bump `SETTINGS_VERSION`, และบล็อก `if (ver < N)`
  ใน `migrateSettings` (บล็อกที่ไม่มี guard `ver <` จะทับค่าที่อาจารย์ตั้งไว้ทุกครั้งที่ bump) · `getSettings()` merge `req` ลึกอีกชั้น
  เป็นตาข่าย (spread ชั้นเดียวจะเอา req เก่ามาแทนทั้งก้อน → ช่องใหม่ `undefined` → เกณฑ์เป็น NaN)
- **วันหมดเทอมคลินิก `LAST_CLINIC_DAY` (26 มี.ค. · `domain/analytics.ts`) เป็นสมมติฐาน** ยังไม่ยืนยันกับภาค · คาบที่เหลือคำนวณผ่าน `periodsLeftNow` ที่เดียว
- ทุกครั้งที่แตะ `domain/` หรือ `data/cloudSync.ts` ให้รัน `npm test` (22 ชุด · `npm test` พิมพ์จำนวนข้อเอง + สุ่ม 2,000 ชุดใน `test:invariants`)

- **คำที่สับสนกันบ่อย:** `procIndex` = ลำดับใน `procList` (ละเอียด นับ sub-step · -1 ถึง ~30) · `progression` = เลขขั้นที่ผู้ใช้เห็น ("5/10" ·
  0 ถึง `maxProgression`) — สองอันนี้ **ไม่เท่ากัน** เอา `procIndex` ไปโชว์ผู้ใช้เมื่อไหร่ ตัวเลขจะไม่ตรงกับที่เห็นทุกหน้า

## กับดักที่เคยทำพังมาแล้ว

- **`completedAt` ของงานที่ `fromSheet: true` เป็นวันนำเข้า ไม่ใช่วันจบจริง** — อะไรที่คำนวณจากวันจบต้องกรอง `fromSheet` ออก ไม่งั้นวันที่ภาคนำเข้าชีต
  ทีเดียวหลายร้อยแถวจะเพี้ยนทั้งกราฟ (เคยเจอใน `burnup`, `durationByType`, `throughputByMonth`)
- **ปีการศึกษาขึ้นวันที่ 1 มิ.ย.** (`lib/date.ts → academicYear` กับ `current_academic_year()` ใน SQL ต้องตรงกัน · `test:rls` ⑫ เทียบให้) ⚠️ เป็นสมมติฐาน ยังไม่ยืนยันกับภาค
- **`isStale` ต้องใช้ `isActiveWork` ไม่ใช่ `!isComplete`** — เคสที่คืนไปแล้วไม่มีใครอัปเดตอีก ดูแค่ "ยังไม่จบ" มันจะพองใน "เคสค้าง" ถาวร · **`npx tsc -b` ใช้แคช incremental** — จะผ่านทั้งที่ของจริงพัง ตรวจจริงด้วย `npm run build` หรือ `npx tsc -b --force`
- **CSV ที่ส่งออกถูกออกแบบมาให้เปิดใน Excel** — ทุกช่องที่ผู้ใช้พิมพ์เองต้องผ่าน `escapeCell()` (เติม `'` หน้าเซลล์ที่ขึ้นต้นด้วย `= + @ -` กัน formula injection)
  เพิ่มคอลัมน์ใน `CSV_COLUMNS` เมื่อไหร่ ห้ามประกอบสตริงเองข้าม escapeCell
- **ห้ามให้ผู้ใช้เจอ "จอขาว" หรือ "หมุนอยู่ตลอดกาล"** — ตาข่ายที่วางไว้: `AppBoundary` (main.tsx) ดักตอนวาดหน้าพัง (โชว์แค่ `err.name` เพราะข้อความ error
  อาจมีข้อมูลผู้ป่วยและผู้ใช้ถูกขอให้แคปส่ง) · ตัวจับเวลา 15 วิ ใน `store/app.ts → init()` ดักตอนเตรียมข้อมูลค้าง (Dexie ถูกบล็อกโดยแท็บอื่น **ไม่ throw และไม่ resolve**)
  · `typeMeta`/`orderOf`/`procList` ทนประเภทงานที่ catalog ไม่รู้จัก **ห้ามถอดตาข่ายพวกนี้ และห้าม `TYPES[x]` แบบ dynamic** (`TYPES[w.type].color` เคยทำจอขาวจากแถวเดียว)
- **ห้ามมี "ป้ายหลอก"** — สถานะที่บอกว่าสำเร็จโดยไม่รู้ผลจริง เคยมีสามตัว (`addPhoto` สร้างแถวเปล่า · `syncNow` ตั้ง `status='ok'`
  ให้รูปทุกใบ · `syncNow` ล้าง "รอส่ง" + ประทับ `syncedAt` โดยไม่เคย `flushNow()`) กติกา: **ถามผลจริงก่อนเปลี่ยนป้าย** —
  ค่าที่บอกว่า "ขึ้นแล้ว" เขียนได้จากที่เดียวคือหลังปลายทางตอบรับ · **บั๊กร้ายแรงเจอตอนลองใช้จริง ไม่ใช่ตอนอ่านโค้ด** กดจริงเสมอก่อนบอกว่าเสร็จ
- **"ต่อเซิร์ฟเวอร์ได้ไหม" ตัดสินจากผลของคำขอจริงที่เดียว** — `syncStatus()` ใน `cloudSync.ts` (`ok` / `down` / `auth`) เปลี่ยนได้จาก `isRefusal` + `noteContact`
  เท่านั้น ห้ามเดาจาก `navigator.onLine` หรือสวิตช์ออฟไลน์ (เดิมหน้าตั้งค่าขึ้นจุดเขียว "ข้อมูลขึ้นเซิร์ฟเวอร์ทันที" ทั้งที่ยิงไม่ถึง) · **PGRST301 = หมดเวลาเข้าสู่ระบบ
  ไม่ใช่การปฏิเสธ** งานต้องคาในคิว ห้ามกัก · ปุ่ม "เข้าสู่ระบบอีกครั้ง" ใช้ `signOutToReLogin()` (ไม่ล้างเครื่อง · เขียนคิวลงดิสก์ก่อน) ห้ามใช้ทางออกจากระบบปกติ ·
  หน้าจอที่ใช้: `SyncProblemsCard` (ใบเดียวสองฝั่ง) · `SyncBanner` หน้าแรก นศ. (**ครั้งละแถบเดียว** · ต่อไม่ติดขึ้นเฉพาะเมื่อของค้างเกิน 1 วัน — ไวไฟหลุดสั้นๆ ห้ามขึ้น) ·
  บรรทัด "ยังไม่ถึงเซิร์ฟเวอร์" ในการ์ดวันนี้ของอาจารย์ · "มีแอปรุ่นใหม่" มาจาก `lib/appUpdate.ts` (service worker ตัวใหม่เข้าคุมหน้า) ไม่บังคับ reload เอง · `test:sync-pg` ⑧ ⑧ข
- **เทสต์ในเบราว์เซอร์ทุกชุดใช้ `bypassCSP: true`** = CSP ของเว็บจริงไม่ถูกทดสอบ (เคยบล็อกรูปงานทุกใบบนเครื่องอาจารย์ — img-src ไม่มี supabase.co)
  เพิ่มของที่โหลดจากโดเมนอื่นเมื่อไหร่ แก้ `vite/csp.ts` แล้วรัน `npm run test:csp-live`
- **`<input type="date" max>` กันได้แค่ปฏิทิน** — พิมพ์ปีเองยังใส่ได้ · วันที่ทุกช่องที่ผู้ใช้แก้ได้ต้องผ่าน `clampPerformedAt()` (`lib/date.ts`) ทั้งใน `repo.ts`
  (`addCheckIn` · `createWorkpieces`) และหน้าจอ ไม่งั้นได้ `performedAt` ปี 2035 ลงฐานจริง แล้วเคสหลุดจากเกณฑ์รายปีเงียบ ๆ ·
  **"ตรงเวลา/สาย" ของเช็คอินต้องมาจาก `checkInStamp()`** (`domain/checkin.ts` · เส้นตัด `PUNCTUAL_CUTOFF`) — เคยมีสามสำเนาแล้วเส้นตัดไม่ตรงกัน
- **`tText()` แทนคำทีละท่อน ห้ามใช้กับประโยคอิสระ** — มีไว้สำหรับ `detail` ของชิ้นงาน · ข้อความ audit **เก็บเป็นไทยเสมอและแสดงดิบ**
  ห้ามผ่าน `t()`/`tText()` (เคยได้ "Saveผลและนัดครั้งNext") — audit เป็นหลักฐาน ต้องอ่านตรงกับที่เก็บ
  · **ชื่อขั้นใน `catalog.ts` เป็นภาษาอังกฤษทั้งไฟล์** — ชื่อขั้นไหลลง audit และผ่าน `tText()`
- **ห้ามอ่านเลขกลุ่มด้วย `code.replace(/\D/g,'')`** (รวมเลขรุ่น: 'TH6-PT10' → 610) ใช้ `groupNumberOf()` / `sortGroupCodes()` · **รูปแบบ id
  ประกอบจากที่เดียว `groupCodeFor()` / `studentIdFor()`** (`domain/group.ts` · repo · rosterApply · นำเข้าชีต ใช้ตัวเดียวกัน) ห้ามต่อสตริง
  `st-…`/`TH…-PT…` เองในหน้าจอ (ลายเซ็นข้อมูลตัวอย่างพึ่งรูปแบบนี้ — หัวข้อถัดไป) · **รุ่นที่รับรายชื่อไว้ล่วงหน้ายังไม่ขึ้นคลินิก** (`isUpcoming`) ห้ามคิดสีเสี่ยง/เขียน "ปี 4" ให้เขา หลักสูตรมีแค่ปี 5–6
- ของเล็กที่เคยพัง: ไอคอน Phosphor ใช้ได้แค่ `regular`/`bold`/`fill` (`duotone` ถูก build ตัดทิ้ง → ไอคอนว่าง · `vite/phosphor-weights.ts`) ·
  ตัวอ่าน .xlsx เขียนเอง (`lib/xlsxRead.ts`) มีเพดานไฟล์/แถว/คอลัมน์กันไฟล์ระเบิด ห้ามถอด และห้ามสลับไปใช้ไลบรารี `xlsx` บน npm (มีช่องโหว่) ·
  error จาก OAuth ที่กลับมากับ URL ห้ามโชว์ดิบ (`lib/auth.ts` map รหัสที่รู้จัก ที่เหลือข้อความกลาง — โชว์ดิบ = หน้า login ถูกยัดข้อความจากลิงก์ปลอมได้)

## ข้อมูลตัวอย่างห้ามแตะเซิร์ฟเวอร์จริง

- **โหมด cloud ห้ามเขียนข้อมูลตัวอย่างลงลิ้นชักเด็ดขาด** — `seedIfEmpty` / `ensureAlumniSeeded` คืนทันทีเมื่อ `cloudEnabled`
  เดิมหน้า "ตั้งค่า" เรียก `ensureAlumniSeeded` ทุกครั้งที่เปิด แล้ว pushAll ดันข้อมูลปลอมเข้าฐานจริง (ผู้ป่วย 2 → 1,143 · HN DEMO-)
  **ฟีเจอร์ใหม่ที่เรียกอะไรใน `seed.ts` ต้องถามก่อนว่า "ในโหมด cloud มันเขียนลงลิ้นชักไหม"**
- เครื่องที่โดนไปก่อนแก้: `purgeLocalDemoRows()` ล้างของปลอมตอนเปิดแอป (ช่วงหยุด sync = ไม่สั่งลบบนตู้) · ลายเซ็น `isDemoStudentId`
  (`st-<กลุ่ม>-<1–8>`) / `isDemoHn` (`DEMO-`) / `isDemoTeacherId` **ต้องไม่ชนกับของจริง** (นักศึกษาจริง = `st-<กลุ่ม>-<รหัส 7 หลัก>`)
  เปลี่ยนรูปแบบ id ฝั่งไหน ต้องแก้ลายเซ็นใน seed.ts + `supabase/check-demo-rows.sql` / `remove-demo-rows.sql` พร้อมกัน (`test:rls` ⑩)
- **ห้ามอ่านชื่อ/กลุ่มของคนที่ล็อกอินจากลิ้นชักแค่ตอน init** — เครื่องใหม่ยังไม่มี students/teachers/groups จนกว่า sync รอบแรกจบ (เดิมได้ชื่อเดโม
  ค้างทั้งเซสชันแล้วจดลง audit) · `refreshActorName()` อ่านใหม่หลัง `initCloudSync()` · ค่าที่ต้องอ่านจากรายชื่อตอนเปิดแอป ให้คิดถึงเครื่องใหม่ก่อน (`test:first-login`)
- **`setSyncPaused` นับชั้น** — ห้ามเปลี่ยนกลับเป็นสวิตช์ตัวเดียว · StrictMode สั่ง init สองรอบพร้อมกัน งานแรกจบแล้วเปิด sync ขณะงานที่สองยังเขียน → คำขอขยะหลายพัน
- **นำเข้าชีตทั้งรุ่น (ลบแล้วเขียนใหม่ทั้ง cohort) เปิดเฉพาะโหมดไม่ต่อ cloud** (`ImportSheet.tsx → cohortModeAllowed`) — ในโหมด cloud การลบพวกนั้น
  sync ขึ้นเซิร์ฟเวอร์ทั้งที่หน้าจอบอก "ไม่ออกจากเครื่อง" · ห้ามเปิดกลับโดยไม่แก้ทั้งข้อความและกติกา
- **ทดสอบหน้าจอจริงในโหมด cloud โดยไม่แตะเซิร์ฟเวอร์จริง** — `scripts/local-supabase.mts` (Postgres จริง + migration ทุกไฟล์ · วิธีรันใน `docs/dev-setup.md`)
  ล็อกอินด้วย session ทดสอบจาก `/auth/v1/dev-session?email=t1@test.local` ใส่ localStorage · **ห้ามพิมพ์รหัสผ่านลงฟอร์มล็อกอินผ่านตัวขับเบราว์เซอร์** แม้เป็นบัญชีทดสอบ

## ล็อกอินด้วย Google

- **ทางหลักคือปุ่ม Google** (Supabase OAuth) · อีเมล+รหัสผ่านเก็บไว้ให้บัญชีสาธิต/สำรอง · ใครเข้าได้ยังตัดสินที่ trigger
  `handle_new_user` (`0009`) ตามรายชื่อเชิญ — Google แค่ยืนยันเจ้าของอีเมล
- **`flowType: 'pkce'` ห้ามเปลี่ยนกลับ** — แอปใช้ HashRouter · แบบ implicit ส่ง `#access_token=` กลับมาชนเส้นทางของแอป ·
  **error ที่กลับมากับ URL ต้องถูกอ่านและลบก่อน `createClient`** (`takeOAuthReturnError` ใน `lib/cloud.ts`) — supabase-js กลืน error เงียบๆ
  และไม่ลบออกจาก URL · trigger ปฏิเสธ = `Database error saving new user` เสมอ · **ห้ามลบ `code`** (supabase-js ใช้แลก session)
- **นักศึกษาผูกบัญชีเอง + อาจารย์ที่ปรึกษายืนยัน** (`0023_link_requests.sql`) — อีเมล **@student.mahidol.edu** (รูปแบบ `ชื่อ.นามสกุลย่อ` ไม่มีรหัส
  นักศึกษา จับคู่อัตโนมัติไม่ได้) ที่ไม่อยู่ในรายชื่อเชิญ สร้างบัญชี "ยังไม่ผูก" ได้ (เห็นอะไรไม่ได้) → ใส่รหัสนักศึกษา → ที่ปรึกษากลุ่มนั้น/หัวหน้ารายวิชากดยืนยัน
  **ห้ามถอดด่านอาจารย์ยืนยัน** (ใส่รหัสเพื่อนแล้วจะเห็นคนไข้ของเพื่อน) · ทุกอย่างผ่าน RPC ตาราง `link_requests` ไม่มี policy
  โดเมนที่ผูกเองได้อยู่ที่ `self_link_email_ok()` ใน SQL ที่เดียว · `0027` จำกัด 5 ครั้งต่อบัญชี (กด "แก้รหัส" ไม่รีเซ็ตตัวนับ —
  กันไล่เดารหัสเก็บชื่อทั้งรายชื่อ) · `test:rls` ⑪ ⑮ · `test:google-login`
- **หน้ารายชื่อ & นำเข้า (`Roster.tsx`) เปิดให้เฉพาะหัวหน้ารายวิชา** — ไฟล์รายชื่อมีคอลัมน์อีเมล (ไม่บังคับ · `rosterParse` อ่านให้) ลงรายชื่อเชิญทันที
- **อาจารย์เลือกกลุ่มที่ปรึกษาเอง · หัวหน้ารายวิชาแก้ได้** (`0024`) · กลุ่มหนึ่งมีที่ปรึกษากี่ท่านก็ได้ · หนึ่งท่านดูแลได้หลายกลุ่ม · ถามเมื่อปีนี้ยังไม่เลือก
  (เด้งจาก `TeacherShell`) · **ขึ้นปีใหม่ล้างที่เลือกทั้งหมด** (`groups.advisor_year` · `reset_advisors_for_new_year()`) · ⚠️ **ที่ปรึกษาเก็บสองที่**
  `groups.advisor_ids` (RLS) กับ `students.advisor_ids` (หน้าจอ) — เขียนผ่าน `claim_group` / `release_group` / `set_group_advisors` เท่านั้น ·
  **อ่านเพื่อตัดสินต้องผ่าน `currentAdvisorIds()` / `current_advisors()`** · กฎ audit ใช้ `my_advised_groups()` · ตัดสินว่า "ไม่มีที่ปรึกษา"
  ได้ต่อเมื่อแถว groups ลงเครื่องครบ (`known`) — เครื่องใหม่ students มาก่อน groups · `test:rls` ⑫
- ⚠️ ยังไม่เคยลองบน iPhone ที่ติดตั้งแอปลงหน้าจอโฮม — กลับจาก Google อาจไปโผล่ใน Safari แทน · **ที่อยู่เว็บ:** Vercel (`vercel.json` ·
  `docs/vercel-setup.md`) ค่อยผูก subdomain คณะทีหลัง · **ช่วงนี้ห้ามชวนใครติดตั้ง PWA ลงหน้าจอโฮม** (เปลี่ยน origin แล้ว IndexedDB/ไอคอนค้างที่เก่า)

## ข้อมูลหาย — ข้อที่แพงที่สุดของระบบนี้

- **คิวรอส่งขึ้นตู้กลางต้องทนการปิดแท็บ** (`cloudSync.ts`) — `dirty`/`pendingDeletes` มีสำเนาในตาราง `kv` คีย์ `syncOutbox`
  เขียนทุกครั้งที่คิวขยับ และอ่านกลับ **ก่อน `pullAll()` ครั้งแรก** (ตัวกัน `skip` ของ pullAll อ่านจากคิวนี้) · `pagehide` เป็น flush สุดท้าย
  (เดิมคิวอยู่ในหน่วยความจำ → กด step ออฟไลน์แล้วปิดแท็บ = ฉบับเก่าบนตู้ทับ แต่แถว `updates` รอด → "ประวัติบอกทำแล้ว เคสบอกยัง" · `test:offline`)
  **ห้ามเอาสำเนานี้ออก · ห้ามให้ `pullAll` ทำงานก่อน `flush`** · `persistOutboxNow` ห้ามเขียนจนกว่าจะอ่านคิวเก่ากลับมาแล้ว
  (ไม่งั้นตอน logout เขียนคิวว่างทับดิสก์) · `stopCloudSync` ต้องล้าง timer ของตัว persist ด้วย
- **`POLICY_VERSION` ใน `cloudSync.ts` สั่งให้ทุกเครื่องดึงใหม่หนึ่งรอบ** — bump เมื่อ migration เปลี่ยน "ใครเห็นแถวไหน" (0021/0024 เคย)
  · **ออกจากระบบแบบล้างเครื่อง (`data/localWipe.ts`) ต้องปฏิเสธถ้ายังมีรูปที่ไม่มี `storagePath`** — ไบต์ยังไม่ขึ้น Storage = หายถาวร
- **`scripts/backup.ts` ต้องครบเท่ากับ `TABLES` ใน `cloudSync.ts` เสมอ** (เคยขาด 5 ตาราง · ไม่มี error ให้ใครเห็น) · ชื่อตารางค่าตั้งคือ
  **`app_settings`** ไม่ใช่ `settings` · `test:offline` ⑦ เทียบให้ · **ไบต์รูปสำรองแยก** จากบักเก็ต `case-photos` · ปุ่มสำรองในแอป
  (`data/fullBackup.ts`) **ไม่รวมรูป** และเขียนบอกบนหน้าจอแล้ว ห้ามลบคำเตือนนั้น — สำเนาที่ไม่ครบโดยคนกดไม่รู้ แย่กว่าไม่มีสำเนา
- **สำเนาที่ไม่เคยลองกู้ ยังไม่นับว่าเป็นสำเนา** — `npm run restore` (ไม่ใส่ธง) ตรวจสำเนาแบบไม่ต่อเน็ต ควรรันทุกเดือน · `--dry-run` เทียบกับเซิร์ฟเวอร์ ·
  `--yes` เขียนจริง · กฎที่ห้ามลืม (เหตุผลเต็มอยู่หัว `scripts/restore.ts` และ README): **① ห้ามกู้ด้วย service key** — `checkin_scoring_guard` (`0017`)
  เช็ค `is_teacher()` จาก `auth.uid()` ที่ service key ไม่มี → ตอน update คงค่าเดิมเงียบ ๆ = คะแนนทุกคาบหาย · **② `audit` ยิงแบบ ignore-duplicates เท่านั้น** ·
  **③ โปรเจกต์ว่างกู้ทันทีไม่ได้** (ต้องมีบัญชีอาจารย์ + แถว `teachers`/`app_users` ก่อน) · **④ ไฟล์รูปใช้ `RESTORE_STORAGE_SERVICE_KEY` แยก** ห้ามใช้กับตาราง ·
  **⑤ แถว `photos` กู้หลังอัปไฟล์เสมอ** · **⑥ `--yes` ถูกปฏิเสธถ้า url ใน `_meta.json` ไม่ตรงปลายทาง** เว้นแต่ตั้ง `RESTORE_URL` ชัดเจน ·
  **กู้รายคน `--student=` เติมเฉพาะแถวที่หาย** ห้ามเปลี่ยนเป็นทับ · `test:restore` + `test:restore-e2e` ล็อกไว้แล้ว — แก้ backup.ts / restore.ts ต้องรันทั้งสองชุด
- **คิวส่งจำ "ช่องที่แก้" ไม่ใช่แค่ "แถวที่แก้"** — middleware อ่านฉบับเดิมด้วย `getMany` **ก่อน** เขียน แล้วเทียบช่องที่ต่าง · ค่าในคิวเป็น `Set<field>`
  = PATCH เฉพาะคอลัมน์นั้น · `null` = ส่งทั้งแถว · **กฎที่ห้ามพลาดถ้าแตะตรงนี้:** `dirty` ชั้นในเป็น **Map** — `skip` ของ `pullAll` ต้องใช้ `.keys()`
  ไม่ใช่ spread (ได้คู่ `[pk, ช่อง]` → pull ทับแถวที่ค้างส่ง) · อ่านฉบับเดิมไม่ได้ = ถือว่า **"ทั้งแถว"** (เดาผิดทางนั้นเสียแบนด์วิดท์ · ทางกลับคือข้อมูลหาย) ·
  PATCH ที่ไม่โดนแถวไหน = ตู้ไม่มีแถวนั้น **ต้อง fallback ไป upsert ทั้งแถว** ไม่ใช่ถือว่าสำเร็จ (`test:clinic` ⑪) · สำเนาคิวบนดิสก์ `v: 2`
  **ต้องอ่านรูปเก่าได้ด้วย** (คนอัปเดตแอปกลางคาบ · `test:offline`) · **"กัก" ได้เฉพาะเมื่อตู้ตอบรหัสปฏิเสธ** (`isRefusal` — SQLSTATE 5 ตัว / PGRSTxxx)
  error ไม่มีรหัส = เน็ตหลุด → รอส่งต่อ **ห้ามนับรอบ ห้ามกัก** (เคยนับรวม: ไวไฟหลุด ~45 วิ = เคสใหม่ถูกกักไม่ขึ้นตู้เลย · ตู้ปลอมในเทสต์ต้องใส่ `code`
  ให้ error ที่เป็นการปฏิเสธ) · **แถวที่ถูกกักต้องถูกข้ามทั้งใน `pullAll` และ realtime** — มันคือฉบับในเครื่องที่ยังไม่เคยขึ้นตู้ ถ้า pull ทับ "ลองส่งใหม่"
  จะส่งฉบับตู้ขึ้นไปแทน · เซิร์ฟเวอร์ยังต้องมีกฎเจ้าของช่อง — `0020` คุม `checkins` (อาจารย์ทับโน้ตนักศึกษาไม่ได้) เพราะกันการยิง API ตรงได้ด้วย
  **เขียนฟีเจอร์ที่สองบทบาทเขียนตารางเดียวกันเมื่อไหร่ ให้คิดถึงข้อนี้ก่อน**
- **`pushAll` ดันเฉพาะแถวที่ตู้ยังไม่มี (`ignoreDuplicates`) และเฉพาะแถวที่การดึงล่าสุดไม่เห็นบนตู้ (`serverKeys`) — ห้ามเปลี่ยนเป็น merge**
  ถูกเรียกหลัง pullAll ทุกครั้งที่เปิดแอป · merge = กด step บนไอแพดระหว่างมือถือกำลังเปิดแอป แล้ว step ถูกย้อน (`test:sync-pg` ⑩) · ส่งทุกแถว = ≈ 1 MB
  ทุกครั้งที่เปิด + ฟื้นแถวที่คนอื่นเพิ่งลบ · ตารางที่ดึงไม่ครบ (error) ไม่มีค่า → ส่งทุกแถว ห้ามเดาว่า "ตู้มีแล้ว"
- **`pullAll` รอบ 15 วิ ดึงเฉพาะแถวที่ขยับ (`updated_at >=` ค่าสูงสุดที่เห็น − 2 นาที) และห้ามวิ่งซ้อน** — เดิมเครื่องอาจารย์ ~600 MB/ชม.
  (`perf:scale` · `test:sync-pg` ⑫) · ห้ามเอาช่วงเผื่อ 2 นาทีออก (now() ของ Postgres คือเวลาเริ่ม transaction) · ตราเวลาอนาคต / รายชื่อ-กลุ่มขยับ
  = ดึงทั้งตาราง · `0027` ใส่ index `updated_at` ให้ทุกตาราง sync — เพิ่มตารางใหม่ต้องใส่ index ด้วย · ⚠️ แถวที่ถูกลบบนตู้ไม่ถูกลบจากเครื่องด้วย pull (มีแค่ realtime) ยังไม่ได้แก้
- **supabase-js upsert หลายแถว: แถวที่ไม่มีช่องที่แถวอื่นในก้อนมี จะได้ NULL ไม่ใช่ default** → ต้องใส่ `{ defaultToNull: false }`
  ตู้ปลอมในเทสต์ไม่รู้จักพฤติกรรมนี้ — เรื่องที่พึ่ง Postgres/PostgREST ทดสอบใน `test:sync-pg` เท่านั้น
- **ประตูที่ข้อมูลออกจากระบบต้องมีด่านเท่ากันทุกบาน** — ตรวจสิทธิ์ตามนโยบาย + จด audit **ก่อน** สร้างไฟล์ ผ่าน `authorizeExport()`
  (`lib/export.ts`) ตัวเดียว: CSV · ปุ่ม "สร้าง PDF" · ปุ่มสำรองข้อมูล (`data/fullBackup.ts`) · ทางออกใหม่ (พิมพ์/แชร์/ดาวน์โหลด) ต้องผ่านด่านเดียวกัน

## ความปลอดภัยของฐานข้อมูล

- **รัน `supabase/security-check.sql` ทุกครั้งหลังรัน migration ใหม่** — statement เดียว อ่านอย่างเดียว
- **เขียน migration ใหม่ ต้องรัน `npm run test:rls` ก่อนให้ผู้ใช้เอาไปรัน** — มันรัน SQL ทุกไฟล์บน Postgres จริงในเครื่อง (PGlite · `scripts/pg-supabase.mts`)
  SQL ที่ไม่เคยรันจริงห้ามส่งให้ผู้ใช้ (เคยจับได้: `check-migrations.sql` ตอบว่า 0020 "ยังไม่ได้รัน" ทั้งที่รันแล้ว เพราะ `like` ไม่ยอมให้เว้นวรรคหลายช่อง)
  ของ Supabase ที่ Postgres เปล่าไม่มี (role / auth / storage) จำลองใน `pg-supabase.mts` — ใช้เพิ่มเมื่อไหร่ต้องเติมที่นั่น · migration ใหม่ต้องเพิ่มร่องรอยใน
  `check-migrations.sql` + `audit.sql` ด้วย · **ลำดับส่ง:** `test:rls` ผ่าน → ผู้ใช้วางไฟล์ใน SQL Editor → `check-migrations.sql` → `security-check.sql`
  **`0027` – `0031` อยู่ในลำดับนี้เหมือนไฟล์อื่น** (รันหรือยัง ดู `docs/status.md`)
  · **เคสที่อาจารย์ประเมินแล้วลบไม่ได้** (0030 · `workpiece_has_teacher_evidence`) — กฎอยู่สองที่และต้องตรงกัน: SQL ตัวนี้ กับ `hasTeacherEvidence` ใน `domain/rules.ts`
    ที่ `deleteWorkpiece` เช็ค **ก่อนแตะอะไรทั้งนั้น** (เซิร์ฟเวอร์กันเคส/ประวัติ/ผู้ป่วย แต่ไม่กันรูป — ปล่อยให้ลบก่อนแล้วรอเซิร์ฟเวอร์ปฏิเสธ = รูปงานหายถาวร) ·
    ประวัติ step (`updates`) แก้ช่องบอกตัวตนย้อนหลังไม่ได้ เติมได้แค่รูป/โน้ต · เลขขั้นใน `workpieces` **ตั้งใจไม่ล็อก** (ปุ่มเลิกทำ · งานออฟไลน์) — เหตุผลใน `docs/survey-post-approval-locks.md` · แทนการล็อก เซิร์ฟเวอร์จดรอยลง `workpiece_step_log` (0031) — ตารางนี้ **ห้ามใส่ใน `TABLES` ของ `cloudSync.ts`**
  · **เช็คอินวันละแถวต่อคนบังคับที่ฐานข้อมูล** (0029 `checkins_student_date_uidx`) — ข้อมูลตัวอย่างในเทสต์ที่รัน migration จริงต้องไม่สร้างคาบซ้ำวันให้คนเดียว · เครื่องที่ถูกปฏิเสธเห็นคำอธิบายในหน้าตั้งค่า (`problemText` ใน `Sync.tsx`)
  · **ที่ปรึกษากลุ่มเขียนผ่านฟังก์ชันเท่านั้น** (0028 `groups_guard_advisors`) — เขียน `groups.advisor_ids` ตรงๆ ด้วยบัญชีที่ไม่ใช่หัวหน้ารายวิชา ค่าเดิมคงอยู่เงียบๆ
  · **แบบประเมินตนเองกู้ได้ทางเดียว** คือ rpc `restore_self_assessments` (0028 · หัวหน้ารายวิชา) — `restore.ts` ใช้ทางนี้แล้ว
- **ไม่มีกฎ UPDATE/DELETE = RLS กรองแถวทิ้งเงียบๆ (0 แถว) ไม่ขึ้น error** — ตรวจสิทธิ์ให้ตรวจที่ "ผล" ไม่ใช่รูปแบบของการปฏิเสธ
  · **RLS กัน update ไม่ได้แปลว่ากัน delete** — คาบที่ประเมินแล้วเคยถูกนักศึกษาลบผ่าน API ได้ (0027 `checkin_delete_guard`) ตารางที่มี "หลักฐาน" ต้องคิดทั้งคู่
- **กฎที่ "ตั้งใจให้อาจารย์" ต้องมี `is_teacher()` อยู่ในกฎจริงๆ** — `my_group()` ของนักศึกษาคืนกลุ่มตัวเอง และ `is_my_student()` เป็น security definer
  (0005 ลืม นักศึกษาจึงอ่าน audit ของเพื่อนได้ — ปิดใน 0021) · **กฎอ่านแบบ `using (true)` ห้ามมีอีก** — บัญชี "ยังไม่ผูก" (0023) เคยอ่าน
  รายชื่ออาจารย์/กลุ่ม/ค่าตั้ง/นโยบายได้ทั้งตาราง (0027 ปิด) · ตารางใหม่ที่ "ใครก็อ่านได้" ต้องเช็ค `my_role()` เสมอ
- **trigger security definer ห้ามทำตามค่าที่ client ส่งมาโดยไม่ตรวจ** — `cleanup_photo_object` เคยลบไฟล์ตาม `storage_path` ที่นักศึกษาเขียนเองได้ (0021 ตรวจตอนเขียน)
- **ฟังก์ชันใหม่ใน `public` กลายเป็น `/rest/v1/rpc/<ชื่อ>` ทันที** และ `anon` เรียกได้ — `revoke ... from public` ไม่พอ ต้อง `from public, anon`
  (0021 ตั้ง default privileges แล้ว แต่ **ห้ามถอดจาก `authenticated`** — RLS ทุกข้อเรียกฟังก์ชันพวกนี้)
- **ห้ามใส่บัญชีทดสอบลงใน migration** — migration รันบนเซิร์ฟเวอร์จริงเสมอ (0003/0005 เคยใส่ `@example.com` · 0027 ลบทิ้งให้เสมอเผื่อมีคนรัน 0003 ซ้ำ)

## ชื่อผู้ป่วย — นำร่องใช้แค่ HN (0026)

- **`pdpa_policy.patient_names` ค่าเริ่มต้น = ปิด** — ตกลงกับภาค: นำร่องไม่เก็บชื่อผู้ป่วย ใช้จริงค่อยเปิด · "ไม่เก็บเลย" ไม่ใช่ "เก็บแต่ซ่อน"
  เปิดได้เฉพาะหัวหน้ารายวิชา (ตั้งค่า → นโยบาย PDPA · มีกล่องยืนยัน) · ด่านสามชั้น ห้ามถอดชั้นไหน: ① trigger `zz_zz_strip_patient_name` ล้าง
  `patients.name` / `sect2|sect3_records.patient_name` ทุกครั้งที่เขียนตอนสวิตช์ปิด (กันแอปรุ่นเก่า/ยิง API ตรง) ② `repo.ts` + นำเข้าชีต ล้างก่อนลงเครื่อง
  (`patientNamesOn()`) ③ หน้าจอ/ไฟล์/ใบพิมพ์ ไม่แสดงชื่อแม้มีค้าง · ชื่อที่ค้างบนเซิร์ฟเวอร์ก่อน 0026 ลบด้วย `supabase/clear-patient-names.sql` (เฉพาะตอนสวิตช์ปิด)
- **แสดงชื่อผู้ป่วยต้องใช้ `patientTitle()` / `patientWithHn()` / `patientLabel(…, level)`** (`lib/privacy.ts`) คู่กับ `usePatientNamesOn()`
  ห้าม `t(patient.name)` ตรงๆ · ปิดอยู่ = "HN 123" เป็นหัวเรื่อง บรรทัดรองบอกเพศ/อายุ · เดโม/โหมดในเครื่อง `DEMO_POLICY.patientNames = true` (ชื่อสมมติ)
- **ระดับปิดบังรายหน้าอยู่ที่ `identityLevelFor(surface, maskByDefault)` ที่เดียว** (`lib/privacy.ts` · อ่านผ่าน `hooks/data.ts` · ค่ามาจาก `pdpa_policy.mask_by_default`)
  ประเมินรายคาบเห็นชื่อย่อ+HN ปิดบัง · หน้าที่ต้องจับคู่แฟ้มจริง (ตรวจงาน · ใบ Section II/III · นำเข้าชีต · เคสตัวเอง) เห็นเต็ม · ภาคเคาะแล้วแก้ที่ตารางนี้
  ห้ามกระจายในหน้าจอ · `test:rls` ⑭ · `test:core` · `test:export` · ⚠️ ยังไม่มีหน้าให้นักศึกษา "เติมชื่อ" ให้เคสเก่าหลังเปิดสวิตช์ — ทำตอนภาคจะเปิดใช้ชื่อจริง

## รูปงาน (ข้อมูลอ่อนไหวที่สุดในระบบ)

- **ไบต์รูปอยู่ที่ Supabase Storage** บักเก็ต `case-photos` (private) — ตาราง `photos` เก็บแค่ `storagePath` · สำเนาในเครื่องอยู่ตาราง Dexie
  `blobs` ซึ่ง **ห้ามใส่ใน `TABLES` ของ `cloudSync.ts` เด็ดขาด** · **ชื่อไฟล์ห้ามมีชื่อคนไข้หรือ HN** — ใช้ `caseCode()` (`lib/privacy.ts`) ·
  โฟลเดอร์แรกต้องเป็น `studentId` เพราะ RLS ใน `0018` อ่านตำแหน่งนั้น
- **`storagePath` คือหลักฐานว่าขึ้นคลาวด์แล้ว ไม่ใช่ `status`** — `'ok'` เขียนได้จากที่เดียวคือหลัง Storage ตอบรับ (`photoStore.uploadOne`)
  · **ห้ามลบของในเครื่องก่อนคลาวด์ยืนยัน** — รูปในปากคนไข้ถ่ายซ้ำไม่ได้ · `pruneLocalCache()` ทิ้งได้เฉพาะใบที่มี `storagePath` (ล้างเครื่องตอน logout ก็กฎเดียวกัน)
- **แนบรูปจากคลังต้องบอกว่าแนบเข้าเคสไหน** และให้เลือกเมื่อมีเคสที่เริ่มแล้วมากกว่าหนึ่ง (`Photos.tsx`) — เคยแนบเงียบๆ เข้าเคสที่เรียงมาก่อน

## ชื่อบทบาท · ข้อความ · สี

- **`is_admin` เรียกว่า "หัวหน้ารายวิชา" ไม่ใช่ "หัวหน้าภาค"** ในทุกข้อความที่ผู้ใช้เห็น · มีได้หลายคน · ตั้งเพิ่มด้วย SQL
  (`update invites/app_users set is_admin = true`) หรือแท็บอาจารย์ในหน้ารายชื่อ · ข้อความ error ใน migration ที่รันแล้วยังเขียน "หัวหน้าภาค" —
  แปลงฝั่งแอปด้วย `serverText()` (lib/link.ts) · คอมเมนต์เก่าใช้คำเดิมได้ · **ชื่อคนจริง/อีเมลจริงห้ามอยู่ในโค้ด** (repo เป็น public)
- **ชื่อคน (นักศึกษา/อาจารย์) บนจอใช้ `personName(x)` ห้าม `t(x.name)`** (`lib/i18n.ts` · 0025) — โหมดอังกฤษได้ `nameEn` ถ้ามี ·
  ของที่ "เก็บ" (audit · ชื่อผู้ประเมิน · `currentActor`) ใช้ `name` ไทยเสมอ
- **นำเข้ารายชื่อ**: ทางหลัก = ปุ่ม "เลือกไฟล์ Excel" อ่านแบบฟอร์ม `public/prostho-roster-request-template.xlsx` ทั้งแท็บนักศึกษา+อาจารย์
  ทั้งไฟล์ / ช่องวางข้อความ / ฟอร์ม "เพิ่มทีละคน" ลงข้อมูลผ่าน `data/rosterApply.ts` ที่เดียว — **ห้ามเขียนทางลงข้อมูลแยก** ·
  แก้หัวตารางในแบบฟอร์ม = `test:user-round` ⑧ ต้องยังผ่าน · อีเมลลงรายชื่อเชิญทันที (ignoreDuplicates — ห้ามเปลี่ยนเป็นทับ) ·
  แถวตัวอย่าง 6604999 ถูกข้าม (`test:user-round` ⑥ · `test:rls` ⑬) · แท็บอาจารย์: แอปดูหัวตาราง (`looksLikeTeacherRoster`) ·
  บทบาทว่าง = ตก ไม่เดาสิทธิ์ · id อาจารย์ = `tc-` + sha256(อีเมลตัวเล็ก) 10 ตัวแรก **ต้องตรงกับ `supabase/add-teacher.sql`** (test:rls เทียบให้)
- **ข้อความใหม่ทุกบรรทัดต้องห่อ `t()`** และเพิ่มคู่คำแปลใน `src/lib/i18n.dict.ts` — ลืมห่อ = โหมดอังกฤษโผล่ไทยปน
  ยกเว้นของที่ "เก็บ": ข้อความ audit และรายการกิจกรรมเช็คอิน (`ACTIVITIES` · `domain/checkin.ts`) เก็บตามที่เขียนไว้
- หัวข้อ radar และชื่อ gate (`Sect II Tx. plan` ฯลฯ) **เป็นอังกฤษทั้งสองภาษา** ตามหัวคอลัมน์ในชีต
- สีประเภทงานเคาะแล้ว (ชุด T6) อยู่ใน `domain/catalog.ts → TYPES` และ `styles/tokens.css` · **ห้ามใช้เขียว/แดง** กับประเภทงาน — จองไว้ให้สถานะผ่าน/เตือน · ป้าย "ยังเป็นข้อมูลสมมติ" ในหน้าอาจารย์ขึ้นเฉพาะ `!cloudEnabled` — ห้ามขึ้นบนเว็บนำร่อง

## รัน & ตรวจ

`npm run dev` · `npm test` · `npm run build` — รายละเอียด (โหมด build ทั้งสาม · local-supabase · ปล่อยรุ่น · migration) ใน `docs/dev-setup.md`

- dev server ให้เปิดผ่าน Browser pane (launch config ใน `.claude/launch.json` เป็นของเครื่อง ไม่อยู่ใน git) **ห้ามรัน dev server ผ่าน Bash**
  · จอกว้างเห็นเป็นกรอบมือถือ (ใช้ตอนนำเสนอ) · แคบกว่า 780px เต็มจอเหมือนใช้จริง · ฝั่งนักศึกษาต้องเช็คที่ **375px** ด้วยเสมอ — เคยล้นขอบจอหลายรอบ
  วัดค่าจริงด้วยสคริปต์ (`getBoundingClientRect().right > clientWidth`) ไม่ใช่ดูรูป
- สลับบทบาทตอนเดโม: แถบ DEMO ด้านบน (จอกว้าง) หรือปุ่มลอย `RoleFab` (มือถือ/iPad) — โหมด cloud สลับได้เฉพาะบัญชีที่มีทั้ง `studentId` และ `teacherId`
- งานที่มีหน้าตา: ทำ mock เทียบให้เลือกก่อนเขียนจริง · ก่อนรันคำสั่งสำรวจเครื่องผู้ใช้ให้บอกก่อน

## เทสต์ — ชุดไหนคุมอะไร

| ชุด | คุมอะไร |
| --- | --- |
| `test:rules` `test:analytics` `test:aggregate` | ตัวเลขทุกตัวบนหน้าอาจารย์ · ห้ามเขียนเทสต์ที่จะแดงเองเมื่อวันเวลาผ่านไป (เคยมี) |
| `test:forms` | สูตรคะแนนใบประเมิน Section II/III |
| `test:import` | นำเข้าชีต (`lib/sheetImport.ts`) — แถวที่อ่านไม่ออกต้องขึ้นรายงาน ไม่เดา |
| `test:settings` `test:conflict` | sync · สองเครื่องเขียนชนกันต้องไม่มีอะไรหายเงียบ |
| `test:groups` | อ่านชั้นปีจากสมาชิก ไม่ใช่จากรหัสกลุ่ม |
| `test:invariants` | สุ่มข้อมูล 2,000 ชุด เช็ค "กฎที่ต้องจริงเสมอ" ข้ามโมดูล · พังแล้วพิมพ์ `seed=` ให้ทำซ้ำ · เพิ่มกฎที่นี่เมื่อเจอ "เลขสองหน้าไม่ตรงกัน" |
| `test:photos` | รูปคนไข้ไม่หายตอนอัปล้ม · ป้าย queue/ok/fail ตรงกับของจริง · ชื่อไฟล์ไม่มี HN |
| `test:core` | privacy · date · cohort · checkin · cheer · sheetImport ตัวจริง — ไฟล์เล็กที่อยู่ใต้ทุกอย่าง |
| `test:export` | ประตูส่งออก: ไม่มีสิทธิ์ = ไม่มีไฟล์ · จด audit ไม่ผ่าน = ไม่มีไฟล์ · ไฟล์ปิดบังต้องไม่มีชื่อ/HN |
| `test:sa` | แบบประเมินตนเอง + การ์ดสรุปให้อาจารย์ — ทุกใบต้องมีตัวเลขจริงกำกับ ไม่เดาแทนคนที่ยังไม่ตอบ |
| `test:sync-button` | ปุ่ม "sync ทันที" ต้องไม่โกหก · รูปผูกกับ step ที่แนบไว้ step เดียว |
| `test:user-round` | บั๊กที่เจอตอนไล่ใช้จริงทั้งสองฝั่ง — วันที่ที่เป็นไปไม่ได้ · ลำดับ/ป้ายกลุ่ม · ตัวกรองรายชื่อ · แบบฟอร์มรายชื่อ |
| `test:offline` | **ข้อมูลหาย** — ออฟไลน์แล้วปิดแท็บ · คิวต้องอยู่รอด · ของที่ลบห้ามฟื้น · `backup.ts` ครบทุกตาราง · ด่านของปุ่มสำรอง |
| `test:restore` | **สำเนากู้กลับได้จริง** — จับสำเนาเสียทุกแบบ · รายการตารางสำรอง = กู้ · วิธียิงตรงกับ trigger จริง |
| `test:clinic` | **คาบจริง 12 เครื่อง + อาจารย์ 2 ท่านพร้อมกัน** — ทั้งกลุ่มเช็คอินนาทีเดียว · อาจารย์ประเมินขณะ นศ. แก้ของตัวเอง · คนเดียวสองเครื่อง |
| `test:rls` | **กฎการเข้าถึงบน Postgres ตัวจริง** (PGlite) — รัน migration ทุกไฟล์แล้วลองเจาะในนามนักศึกษา/อาจารย์/คนนอก · ⑨ พิสูจน์ว่าก่อน 0021 ช่องเปิดจริง · ⑮ ช่องที่ 0027 ปิด (ลบคาบที่ประเมินแล้ว · บัญชียังไม่ผูกอ่านตารางกลาง · เพดาน request_link · บัญชี @example.com) |
| `test:sync-pg` | **sync หลายเครื่องบน Postgres ตัวจริง** — cloudSync.ts ตัวจริง + ตู้กลางคือ PGlite · พฤติกรรม supabase-js ลอกจากไลบรารี (`scripts/pg-postgrest.mts`) · **ผลขัดกับชุดตู้ปลอมเมื่อไหร่ ให้เชื่อชุดนี้** |
| `test:sync-gaps` | **รูในตาข่ายที่ `mutate:sync` หาเจอ** — ทุกข้อต้องตกกับบั๊กรหัส Mxx ที่มันเฝ้า |
| `test:restore-e2e` | **ซ้อมกู้ครบวงจร** — backup.ts + restore.ts ตัวจริงเป็น process แยก กับ Supabase จำลอง · กู้รายคนต้องไม่ย้อนงานของคนอื่น |

เครื่องมือช้าที่ไม่อยู่ใน `npm test` (ต้อง `npx playwright install chromium` ก่อน · รายละเอียดใน `docs/dev-setup.md`):
`test:upgrade` (ซ้อมอัปเดตจากรุ่นเก่าจริง · **เพิ่มรุ่นใน `RELEASES` ทุกครั้งที่รูปแบบข้อมูลในเครื่องเปลี่ยน**) ·
`test:first-login` (รันเมื่อแตะ `store/app.ts` หรือลำดับ init/sync) · `test:google-login` (แตะ `lib/cloud.ts` · `lib/auth.ts` · `Login.tsx`) ·
`test:csp-live` (แตะ `vite/csp.ts` หรือเพิ่มของจากโดเมนอื่น) · `perf:scale` · `mutate:sync` (41 จุด · **หาจุดใส่บั๊กไม่เจอ ให้แก้รายการในไฟล์ ไม่ใช่ลบทิ้ง**)

- `test:export` กับ `test:conflict` ก๊อปไฟล์ต้นฉบับไป temp แล้วสับ import ฝั่งเบราว์เซอร์เป็นของปลอม — **เปลี่ยน import ในไฟล์ต้นฉบับเมื่อไหร่
  เทสต์จะฟ้องว่าหา import ไม่เจอ** ให้ไปแก้รายการ swap() ในเทสต์ · `test:conflict` กับ `test:clinic` จำลองกติกาของ `0017` + `0020` ไว้ในตู้กลางปลอม —
  **แก้ `0017`/`0020` เมื่อไหร่ต้องมาแก้ทั้งสองไฟล์ด้วย** (`test:clinic` ⑧ เทียบรายชื่อช่องให้อัตโนมัติ) · ของใช้ร่วมอยู่ที่ `scripts/test-helpers.mts` ·
  ส่วน UI ยังไม่มีเทสต์ ต้องลองใช้จริงเอา
