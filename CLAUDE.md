# คู่มือสำหรับ Claude — Prostho Tracker

อ่านไฟล์นี้ก่อนแตะโค้ด ส่วนรายละเอียดทั้งหมดอยู่ใน `README.md` (364 บรรทัด) — ไฟล์นี้บอกแค่
"ต้องรู้อะไรก่อนลงมือ" กับ "กับดักที่เคยทำให้พังมาแล้ว"

## นี่คืออะไร

PWA ติดตามความคืบหน้าเคสทันตกรรมประดิษฐ์ วิชา DTPT502 ภาควิชาทันตกรรมประดิษฐ์ ม.มหิดล
ใช้แทนการกรอก Google Sheets เดิม · React + TypeScript + Vite + Dexie(IndexedDB) + Supabase
สองฝั่ง: **นักศึกษา** (มือถือ ออฟไลน์ได้) และ **อาจารย์** (จอใหญ่ dashboard ทั้งชั้นปี)

ผู้ใช้ = ผู้พัฒนาเอง เป็นนักศึกษาทันตแพทย์ ไม่ใช่โปรแกรมเมอร์อาชีพ — **อธิบายเป็นภาษาคน**
บอกผลกระทบก่อนบอกชื่อฟังก์ชัน · คุยภาษาไทย

## กฎที่ห้ามฝ่าฝืน

1. **ยังไม่เคยมีข้อมูลผู้ป่วยจริงในระบบ** ทุกอย่างใน repo เป็นข้อมูลสมมติ — ห้ามเขียนอะไรที่
   ทำให้เข้าใจว่าใช้กับคนไข้จริงแล้ว
2. **ห้ามใส่ชื่อหรือ HN ผู้ป่วยลงในข้อความ audit log** — แถว audit ลบไม่ได้ตามการออกแบบ
   ใส่แล้วข้อมูลจะอยู่เกินกำหนดเก็บถาวรและลบตาม retention ไม่ได้ ใช้รหัสเคสจาก `lib/privacy.ts` แทน
3. **สวิตช์ PDPA ทุกตัวค่าเริ่มต้นคือปิด** (`pdpa_policy` · `0016_pdpa.sql`) ห้ามเปลี่ยนค่าเริ่มต้น
   หรือลดความเข้มของ trigger ใน `0009`/`0016` — คณะยังไม่อนุมัติ
4. **ห้ามแก้ migration ที่รันไปแล้ว** ต้องแก้ = เขียนไฟล์ใหม่เลขถัดไป
   **สถานะบนเซิร์ฟเวอร์ — ยืนยันด้วย `supabase/check-migrations.sql` 13 ก.ย. 69:**
   `0003` · `0009` · `0010` · `0012` · `0014` · `0016` · `0017` · `0018` · `0019` **รันแล้วครบ ✓**
   `0020` (เจ้าของช่องของคาบ) **ยังไม่รัน** — เขียน 12 ก.ย. เป็นตัวเดียวที่ค้าง
   ⚠️ **ห้ามเดาสถานะจากไฟล์นี้** — โปรเจกต์รัน SQL ด้วยมือ ไม่มีตารางบันทึกเวอร์ชัน
   ให้ผู้ใช้ก๊อป `supabase/check-migrations.sql` ไปรันแล้วอ่านผล (statement เดียว
   จึงไม่ถูกกลบเหมือน `audit.sql`) · เคยเขียนผิดในไฟล์นี้มาสองรอบเพราะเชื่อความจำ
5. **ห้าม `git add -A`** — ผู้ใช้เปิดหลายเซสชันพร้อมกันได้ ให้ `git add` เฉพาะไฟล์ที่ตัวเองแก้
   เช็ค `git status` ก่อนเสมอ ถ้ามีไฟล์แปลกหน้าโผล่มาระหว่างทำงาน = มีเซสชันอื่นอยู่ ให้บอกผู้ใช้
6. **commit ในเครื่องได้ แต่ห้าม push** — ผู้ใช้กด Push origin เองใน GitHub Desktop
   ข้อความ commit เป็นภาษาไทย เล่าว่า *ผู้ใช้เจออะไร* ไม่ใช่ *แก้ฟังก์ชันไหน*
7. **ห้ามพูดเรื่องเกรด** ในทุกข้อความที่อาจารย์เห็น — ระบบนี้ไม่ได้ตัดเกรด และห้ามใส่เรื่อง Payment

## ตัวเลขต้องตรงกันทุกหน้า

นี่คือหัวใจของโปรเจกต์ บั๊กที่แพงที่สุดไม่ใช่จอขาว แต่คือ **หน้าอาจารย์ขึ้นเลขสวย ๆ ที่ผิด**
แล้วอาจารย์เอาไปตัดสินใจว่าจะไปตามนักศึกษาคนไหน

- **กติกาทั้งหมดอยู่ใน `src/domain/` เท่านั้น** — ห้ามคำนวณเกณฑ์/สถานะ/เปอร์เซ็นต์ในไฟล์ `routes/`
  ถ้าต้องนับอะไรใหม่ ให้เพิ่มฟังก์ชันใน `domain/` แล้วให้หน้าจอเรียก ไม่งั้นอีกหน้าจะนับคนละแบบ
- **`domain/catalog.ts` คือ source of truth** ของรายการ procedure ห้าม hard-code ชื่อ step หรือจำนวนขั้น
- **ห้าม hard-code เลข 10** เป็นขั้นสุดท้าย — งาน Recall จบที่ขั้น **3** ใช้ `maxProgression(w)` เสมอ
- **เกณฑ์มีสองชุด ห้ามสลับกัน** (`domain/catalog.ts`)
  `CUM_REQ_TYPES` = เกณฑ์สะสม (CD · RPD · PC · CB · **RRM · RFX**) · `REQ_TYPES` = เกณฑ์รายปี (4 ตัวแรก)
  Recall เข้าเกณฑ์สะสมตั้งแต่ 10 ก.ย. 69 แต่ไม่เข้าเกณฑ์รายปี — `countsTowardRequirement` อ่านชุดสะสม
  ส่วน `completedInYear` / `countsForYearlyReq` อ่านชุดรายปี
- **เพิ่มช่องใน `Settings.req` ต้องทำสามที่พร้อมกัน** — `DEFAULT_SETTINGS`, bump `SETTINGS_VERSION`,
  และเพิ่มบล็อก `if (ver < N)` ใน `migrateSettings` · `getSettings()` merge `req` ลึกอีกชั้นเป็นตาข่ายกันไว้แล้ว
  (spread ชั้นเดียวจะเอา req ที่เก็บไว้มาแทนทั้งก้อน → ช่องใหม่เป็น `undefined` แล้วเกณฑ์กลายเป็น NaN)
- ทุกครั้งที่แตะ `domain/` หรือ `data/cloudSync.ts` ให้รัน `npm test` (18 ชุด · 924 ข้อที่พิมพ์ออกมา
  บวกการสุ่ม 2,000 ชุดข้อมูลใน `test:invariants`)

### คำที่สับสนกันบ่อย

| คำ | หมายถึง | ช่วง |
| --- | --- | --- |
| `procIndex` | ลำดับใน `procList` (ละเอียด นับ sub-step) | -1 ถึง ~30 |
| `progression` | เลขขั้นที่ผู้ใช้เห็น ("5/10") | 0 ถึง `maxProgression` |

สองอันนี้ **ไม่เท่ากัน** เอา `procIndex` ไปโชว์ผู้ใช้เมื่อไหร่ ตัวเลขจะไม่ตรงกับที่เห็นทุกหน้า

## กับดักที่เคยทำพังมาแล้ว

- **`completedAt` ของงานที่ `fromSheet: true` เป็นวันนำเข้า ไม่ใช่วันจบจริง** (ชีตไม่มีคอลัมน์นั้น)
  อะไรที่คำนวณจากวันจบต้องกรอง `fromSheet` ออก ไม่งั้นวันที่ภาคนำเข้าชีตทีเดียวหลายร้อยแถว
  ตัวเลขจะเพี้ยนทั้งกราฟ — เคยเจอมาแล้วสามที่ (`burnup`, `durationByType`, `throughputByMonth`)
- **ปีการศึกษาขึ้นวันที่ 1 มิ.ย.** (`lib/date.ts → academicYear`) — ⚠️ นี่เป็นสมมติฐาน ยังไม่ยืนยันกับภาค
- **`isStale` ต้องใช้ `isActiveWork` ไม่ใช่ `!isComplete`** — เคสที่คืนไปแล้วไม่มีใครอัปเดตอีก
  ถ้าดูแค่ "ยังไม่จบ" มันจะพองอยู่ใน "เคสค้าง" ถาวรไม่มีวันลด
- **`npx tsc -b` ใช้แคช incremental** — จะผ่านทั้งที่ของจริงพัง ตรวจจริงด้วย `npm run build`
  หรือ `npx tsc -b --force`
- **ไฟล์ CSV ที่ส่งออกถูกออกแบบมาให้เปิดใน Excel** — ทุกช่องที่มาจากที่ผู้ใช้พิมพ์เอง
  ต้องผ่าน `escapeCell()` ซึ่งเติม `'` นำหน้าเซลล์ที่ขึ้นต้นด้วย `= + @ -` (กัน formula injection)
  เพิ่มคอลัมน์ใหม่ใน `CSV_COLUMNS` เมื่อไหร่ ห้ามประกอบสตริงเองข้าม escapeCell
- **ห้ามให้ผู้ใช้เจอ "จอขาว" หรือ "หมุนอยู่ตลอดกาล"** — สองสภาพนี้ไม่มีทางออกจากมุมผู้ใช้
  ตาข่ายที่วางไว้แล้ว: `AppBoundary` (main.tsx) ดักตอนวาดหน้าพัง ·
  ตัวจับเวลา 15 วิ ใน `store/app.ts → init()` ดักตอนเตรียมข้อมูลค้าง (Dexie ถูกบล็อกโดยแท็บอื่น
  **ไม่ throw และไม่ resolve** มันรอเงียบ ๆ ตลอดกาล) · `typeMeta`/`orderOf`/`procList`
  ทนประเภทงานที่ catalog ไม่รู้จัก **ห้ามถอดตาข่ายพวกนี้ออก และห้ามเพิ่ม `TYPES[x]` แบบ dynamic**
  (ใช้ `typeMeta(x)` เสมอ — `TYPES[w.type].color` เคยทำให้จอขาวจากแถวเดียว)
- **ห้ามมี "ป้ายหลอก"** — สถานะที่บอกว่าสำเร็จโดยไม่รู้ผลจริง เคยมีมาแล้วสามตัว
  (`addPhoto` สร้างแถวเปล่า · `syncNow` ตั้ง `status='ok'` ให้รูปทุกใบ · `syncNow` ล้างรายการ
  "รอส่ง" + ประทับ `syncedAt` โดยไม่เคยเรียก `flushNow()`) กติกา: **ถามผลจริงก่อนเปลี่ยนป้าย**
  ค่าที่บอกว่า "ขึ้นแล้ว" ต้องเขียนได้จากที่เดียวคือหลังปลายทางตอบรับ
- **บั๊กร้ายแรงเจอตอนลองใช้จริง ไม่ใช่ตอนอ่านโค้ด** — เปิดแอปในเบราว์เซอร์แล้วกดจริงเสมอ
  ก่อนบอกว่าเสร็จ (ดูหัวข้อล่าง)
- **`<input type="date" max>` กันได้แค่ปฏิทิน** — พิมพ์ปีเองบนเดสก์ท็อปยังใส่ได้ และไม่มี `<form>`
  ไหนอ่าน validity · วันที่ทุกช่องที่ผู้ใช้แก้ได้ต้องผ่าน `clampPerformedAt()` (`lib/date.ts`)
  ไม่งั้นได้ `performedAt` ปี 2035 ลงฐานจริง แล้วเคสหลุดจากเกณฑ์รายปีเงียบ ๆ (เจอ 10 ก.ย. 69)
- **`tText()` แทนคำทีละท่อน ห้ามเอาไปใช้กับประโยคอิสระ** — มันมีไว้สำหรับ `detail` ของชิ้นงาน
  ที่ประกอบจากท่อนที่รู้จัก · เอาไปครอบข้อความ audit ได้ผลเป็นข้อความปนภาษา
  ("Saveผลและนัดครั้งNext") และ audit เป็นหลักฐาน ต้องอ่านตรงกับที่เก็บ → แสดงดิบเสมอ
- **ชื่อขั้นใน `catalog.ts` เป็นภาษาอังกฤษทั้งไฟล์** ห้ามเขียนชื่อขั้นเป็นไทย
  ชื่อขั้นไหลลง audit log และผ่าน `tText()` ด้วย (เหตุผลข้อบน)
- **ห้ามอ่านเลขกลุ่มด้วย `code.replace(/\D/g,'')`** — มันรวมเลขรุ่นเข้าไปด้วย ('TH6-PT10' → 610)
  ใช้ `groupNumberOf()` / `sortGroupCodes()` ใน `domain/group.ts`
- **รุ่นที่รับรายชื่อไว้ล่วงหน้ายังไม่ขึ้นคลินิก** (`isUpcoming`) — ห้ามคิดสีเสี่ยง/เขียน "ปี 4" ให้เขา
  หลักสูตรนี้มีแค่ปี 5–6 · หน้ารายชื่อรองรับการนำเข้ารุ่นถัดไปก่อน 1 มิ.ย. อยู่แล้ว

## ข้อมูลหาย — ข้อที่แพงที่สุดของระบบนี้

- **คิวรอส่งขึ้นตู้กลางต้องทนการปิดแท็บ** (`cloudSync.ts`) — `dirty`/`pendingDeletes` มีสำเนา
  ในตาราง `kv` คีย์ `syncOutbox` เขียนทุกครั้งที่คิวขยับ และอ่านกลับ **ก่อน `pullAll()` ครั้งแรก**
  เพราะตัวกัน `skip` ของ pullAll อ่านจากคิวนี้ · มี `pagehide` เป็นตัว flush สุดท้าย
  (เดิมคิวอยู่แค่ในหน่วยความจำ → กด step ตอนออฟไลน์แล้วปิดแท็บ = ฉบับเก่าบนตู้ทับทิ้ง
  แต่แถว `updates` รอด จึงได้ "ประวัติบอกว่าทำแล้ว แต่เคสบอกว่ายังไม่ทำ" · พิสูจน์ไว้ใน `test:offline`)
  **ห้ามเอาสำเนานี้ออก และห้ามสลับให้ `pullAll` ทำงานก่อน `flush`**
- **`scripts/backup.ts` ต้องครบเท่ากับ `TABLES` ใน `cloudSync.ts` เสมอ** — เพิ่มตารางในแอป
  แล้วลืมเพิ่มที่นั่น = ตารางนั้นไม่เคยมีสำเนา และไม่มี error ให้ใครเห็น
  (เคยขาด `self_assessments` / `sect2_records` / `sect3_records` / `app_settings` / `pdpa_policy`)
  ชื่อตารางค่าตั้งคือ **`app_settings`** ไม่ใช่ `settings` · `test:offline` ข้อ ⑦ เทียบให้แล้ว
- **ไบต์รูปต้องถูกสำรองแยก** — `backup.ts` ดึงจากบักเก็ต `case-photos` ด้วย
  ปุ่มสำรองข้อมูลในแอป (`data/fullBackup.ts`) **ไม่รวมรูป** และเขียนบอกไว้บนหน้าจอแล้ว
  ห้ามลบคำเตือนนั้น — สำเนาที่ไม่ครบโดยคนกดไม่รู้ แย่กว่าไม่มีสำเนา
- **สำเนาที่ไม่เคยลองกู้ ยังไม่นับว่าเป็นสำเนา** — `npm run restore` (ไม่ใส่ธงอะไร) ตรวจสำเนา
  แบบไม่ต่อเน็ต ปลอดภัยเสมอ ควรรันทุกเดือน · `--dry-run` เทียบกับเซิร์ฟเวอร์ · `--yes` เขียนจริง
  กฎที่ห้ามลืมสามข้อ (เหตุผลเต็มอยู่หัวไฟล์ `scripts/restore.ts` และใน README):
  **① ห้ามกู้ด้วย service key** — trigger `checkin_scoring_guard` (`0017`) เช็ค `is_teacher()`
  จาก `auth.uid()` ที่ service key ไม่มี → ตอน update มันคงค่าเดิมไว้เงียบ ๆ = คะแนนทุกคาบหาย
  **② `audit` ยิงแบบ ignore-duplicates เท่านั้น** (trigger `audit_no_change` ห้าม update)
  **③ โปรเจกต์ว่างกู้ทันทีไม่ได้** — ต้องมีบัญชีอาจารย์ + แถว `teachers`/`app_users` ก่อน
  `test:restore` ล็อกทั้งสามข้อไว้แล้ว รวมถึงว่ารายการตารางของ `backup.ts` = `restore.ts`
- **คิวส่งจำ "ช่องที่แก้" ไม่ใช่แค่ "แถวที่แก้"** (ทำ 13 ก.ย. 69 · `cloudSync.ts`)
  middleware อ่านฉบับเดิมด้วย `getMany` **ก่อน** เขียน แล้วเทียบว่าช่องไหนต่าง
  ค่าในคิวเป็น `Set<field>` = PATCH เฉพาะคอลัมน์นั้น · `null` = ส่งทั้งแถว (แถวใหม่ / คิวรุ่นเก่า)
  **กฎที่ห้ามพลาดถ้าแตะตรงนี้:**
  · `dirty` ชั้นในเป็น **Map** แล้ว — `skip` ของ `pullAll` ต้องใช้ `.keys()` ไม่ใช่ spread ทั้ง Map
    (spread ได้คู่ `[pk, ช่อง]` → skip ไม่ตรง pk → pull ทับแถวที่ยังค้างส่ง = บั๊กข้อมูลหายกลับมา)
  · อ่านฉบับเดิมไม่ได้ = ต้องถือว่า **"ทั้งแถว"** ไม่ใช่ "ไม่มีอะไรแก้"
    เดาผิดทางนั้นเสียแค่แบนด์วิดท์ · เดาผิดทางกลับคือข้อมูลหาย
  · PATCH ที่ไม่โดนแถวไหนเลย = ตู้กลางไม่มีแถวนั้น **ต้อง fallback ไป upsert ทั้งแถว**
    ไม่ใช่ถือว่าสำเร็จ (ไม่งั้นแถวที่ถูกลบบนตู้ทำให้งานผู้ใช้หายเงียบ — `test:clinic` ข้อ ⑪)
  · สำเนาคิวบนดิสก์เป็น `v: 2` · **ต้องอ่านรูปเก่าได้ด้วย** เพราะคนอัปเดตแอปกลางคาบ
    มีสำเนารุ่นเก่าค้างอยู่ (`test:offline` ตรวจทั้งสองรูป)
  · ตารางฝั่งเซิร์ฟเวอร์ยังต้องมีกฎเจ้าของช่องอยู่ดี — `0020` คุม `checkins`
    (อาจารย์ทับโน้ตของนักศึกษาไม่ได้) เพราะกฎที่ฐานข้อมูลกันการยิง API ตรงได้ด้วย
  **เขียนฟีเจอร์ที่มีสองบทบาทเขียนตารางเดียวกันเมื่อไหร่ ให้คิดถึงข้อนี้ก่อน**
- **ประตูที่ข้อมูลออกจากระบบต้องมีด่านเท่ากันทุกบาน** — หัวหน้าภาค + ภาคเปิดสิทธิ์แบบมีชื่อ
  + จด audit **ก่อน** สร้างไฟล์ (`lib/export.ts` กับ `data/fullBackup.ts` ใช้กติกาเดียวกัน)

## รูปงาน (ข้อมูลอ่อนไหวที่สุดในระบบ)

- **ไบต์รูปอยู่ที่ Supabase Storage** บักเก็ต `case-photos` (private) — ตาราง `photos` เก็บแค่ `storagePath`
  สำเนาในเครื่องอยู่ตาราง Dexie `blobs` ซึ่ง **ห้ามใส่ใน `TABLES` ของ `cloudSync.ts` เด็ดขาด**
- **`storagePath` คือหลักฐานว่าขึ้นคลาวด์แล้ว ไม่ใช่ `status`** — `status` เป็นแค่ป้ายที่สะท้อนตามหลัง
  ค่า `'ok'` เขียนได้จากที่เดียวคือหลัง Storage ตอบรับ (`photoStore.uploadOne`) ห้ามตั้งจากที่อื่น
  (เคยมีป้ายหลอกมาสองรอบ: `addPhoto` รุ่นแรกสร้างแถวเปล่า · `syncNow` ตั้ง `ok` ให้ทุกใบโดยไม่ส่งอะไร)
- **ห้ามลบของในเครื่องก่อนคลาวด์ยืนยัน** — รูปถ่ายจากปากคนไข้ ถ่ายซ้ำไม่ได้
  `pruneLocalCache()` ทิ้งได้เฉพาะใบที่มี `storagePath` แล้วเท่านั้น
- **ชื่อไฟล์ห้ามมีชื่อคนไข้หรือ HN** — ใช้ `caseCode()` จาก `lib/privacy.ts`
  โฟลเดอร์แรกต้องเป็น `studentId` เพราะ RLS ใน `0018` อ่านตำแหน่งนั้น

## ข้อความและสี

- **ข้อความใหม่ทุกบรรทัดต้องห่อ `t()`** และเพิ่มคู่คำแปลใน `src/lib/i18n.dict.ts`
  ลืมห่อ = โหมดอังกฤษจะโผล่ภาษาไทยปนออกมา
- หัวข้อ radar และชื่อ gate (`Sect II Tx. plan` ฯลฯ) **เป็นอังกฤษทั้งสองภาษา** ตามหัวคอลัมน์ในชีต
- สีประเภทงานเคาะแล้ว (ชุด T6) อยู่ใน `domain/catalog.ts → TYPES` และ `styles/tokens.css`
  **ห้ามใช้เขียว/แดง** กับประเภทงาน — จองไว้ให้สถานะผ่าน/เตือน

## รัน & ตรวจ

```bash
npm run dev
```

```bash
npm test
```

```bash
npm run build
```

- dev server ให้เปิดผ่าน Browser pane (`.claude/launch.json` มี `prostho-tracker` / `prostho-demo` / `prostho-share`)
  **ห้ามรัน dev server ผ่าน Bash**
- จอกว้างจะเห็นเป็นกรอบมือถือ (ใช้ตอนนำเสนอ) · แคบกว่า 780px จะเต็มจอเหมือนใช้จริง
- ฝั่งนักศึกษาต้องเช็คที่ **375px** ด้วยเสมอ — เคยมีบรรทัดล้นขอบจอหลายรอบ
  วิธีที่ได้ผลคือวัดค่าจริงด้วยสคริปต์ (`getBoundingClientRect().right > clientWidth`) ไม่ใช่ดูรูป
- สลับบทบาทตอนเดโม: แถบ DEMO ด้านบน (เห็นเฉพาะจอกว้าง) หรือแก้ `kv.session` ใน IndexedDB ตรง ๆ

## เทสต์ — ชุดไหนคุมอะไร

| ชุด | คุมอะไร |
| --- | --- |
| `test:rules` `test:analytics` `test:aggregate` | ตัวเลขทุกตัวบนหน้าอาจารย์ |
| `test:forms` | สูตรคะแนนใบประเมิน Section II/III |
| `test:import` | นำเข้าชีต — แถวที่อ่านไม่ออกต้องขึ้นรายงาน ไม่เดา |
| `test:settings` `test:conflict` | sync · สองเครื่องเขียนชนกันต้องไม่มีอะไรหายเงียบ |
| `test:groups` | อ่านชั้นปีจากสมาชิก ไม่ใช่จากรหัสกลุ่ม |
| `test:invariants` | สุ่มข้อมูล 2,000 ชุด เช็ค "กฎที่ต้องจริงเสมอ" ข้ามโมดูล |
| `test:photos` | รูปคนไข้ไม่หายตอนอัปล้ม · ป้าย queue/ok/fail ตรงกับของจริง · ชื่อไฟล์ไม่มี HN |
| `test:core` | privacy · date · cohort · checkin · cheer — ห้าไฟล์เล็กที่อยู่ใต้ทุกอย่าง |
| `test:export` | ประตูส่งออก: ไม่มีสิทธิ์ = ไม่มีไฟล์ · จด audit ไม่ผ่าน = ไม่มีไฟล์ · ไฟล์ปิดบังต้องไม่มีชื่อ/HN |
| `test:sa` | แบบประเมินตนเอง + การ์ดสรุปให้อาจารย์ — ทุกใบต้องมีตัวเลขจริงกำกับ ไม่เดาแทนคนที่ยังไม่ตอบ |
| `test:sync-button` | ปุ่ม "sync ทันที" ต้องไม่โกหก · รูปผูกกับ step ที่แนบไว้ step เดียว |
| `test:user-round` | บั๊กที่เจอตอนไล่ใช้จริงทั้งสองฝั่ง 10 ก.ย. 69 — วันที่ที่เป็นไปไม่ได้ · ลำดับ/ป้ายกลุ่ม · ตัวกรองรายชื่อ · สีเสี่ยงของรุ่นที่ยังไม่ขึ้นคลินิก |
| `test:offline` | **ข้อมูลหาย** — ทำงานตอนไม่มีเน็ตแล้วปิดแท็บ · คิวต้องอยู่รอด · ของที่ลบห้ามฟื้น · `backup.ts` ต้องครบทุกตาราง · ด่านของปุ่มสำรองข้อมูล |
| `test:restore` | **สำเนากู้กลับได้จริง** — จับสำเนาเสียทุกแบบ · รายการตารางสำรอง = กู้ · วิธียิงตรงกับ trigger จริง |
| `test:clinic` | **คาบจริง 12 เครื่อง + อาจารย์ 2 ท่านพร้อมกัน** — ทั้งกลุ่มเช็คอินนาทีเดียว · อาจารย์ประเมินขณะ นศ. แก้ของตัวเอง · คนเดียวสองเครื่อง |

`test:invariants` คือชุดที่จับ "เลขสองหน้าไม่ตรงกัน" — เพิ่มกฎใหม่ที่นั่นเมื่อเจอบั๊กแนวนี้
พังแล้วมันพิมพ์ `seed=` ให้ เอาไปสร้างชุดข้อมูลเดิมซ้ำได้เป๊ะ

`test:export` กับ `test:conflict` ใช้วิธีเดียวกัน: ก๊อปไฟล์ต้นฉบับไป temp แล้วสับ import
ฝั่งเบราว์เซอร์เป็นของปลอม — โค้ดที่ถูกทดสอบคือของจริงทุกบรรทัด **เปลี่ยน import ในไฟล์ต้นฉบับ
เมื่อไหร่ เทสต์จะฟ้องเองว่าหา import ไม่เจอ** ให้ไปแก้ที่รายการ swap() ในเทสต์

`test:conflict` กับ `test:clinic` จำลองกติกาของ `0017_conflict.sql` + `0020_checkin_field_owner.sql`
ไว้ในตู้กลางปลอม — **แก้ `0017`/`0020` เมื่อไหร่ต้องมาแก้ทั้งสองไฟล์ด้วย** ไม่งั้นเทสต์ผ่าน
ทั้งที่ของจริงพัง · `test:clinic` ข้อ ⑧ เทียบรายชื่อช่องระหว่าง SQL กับตู้ปลอมให้อัตโนมัติแล้ว

## ที่ยังค้างอยู่ (อย่าเผลอทำเป็นว่าเสร็จแล้ว)

- `0020` (อาจารย์ทับโน้ตของนักศึกษาไม่ได้) **ยังไม่รัน** — ตัวเดียวที่ค้างบนเซิร์ฟเวอร์
  (ที่เหลือยืนยันแล้วว่ารันครบ 13 ก.ย. 69)
- ยังไม่เคยนำร่องกับ**คนจริง**หลายเครื่องพร้อมกัน (จำลองไว้แล้วใน `test:clinic` — 12 เครื่อง
  + อาจารย์ 2 ท่าน · เจอบั๊กสองตัว แก้หนึ่ง บันทึกข้อจำกัดหนึ่ง)
- 8 ข้อ PDPA ที่ต้องให้ภาค/คณะตัดสินก่อนรับข้อมูลจริง (ท้าย README) — ข้อ 1–3 บล็อกอยู่
- แบบประเมินรวมต่อเคสแยกตามประเภทงาน — รอผู้ใช้ส่งฟอร์มมาให้ครบ
- การปิดบังชื่อ/HN ในหน้าจอ: เครื่องมือพร้อม (`lib/privacy.ts`) แต่ยังไม่ต่อเข้าหน้าไหน
