# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# build เวอร์ชันแชร์ (ไฟล์เดียว) — สร้างใหม่ได้ด้วย npm run build:share
dist-share

# session config ของเครื่อง
.claude

# ไฟล์สำรองข้อมูล — ห้ามขึ้น GitHub เด็ดขาด (มีข้อมูลผู้ป่วย)
backups

# ── กุญแจและความลับ — ห้ามหลุดขึ้น repo เด็ดขาด (repo นี้เป็นสาธารณะ) ──
# เดิมกันได้ด้วย *.local แบบอ้อมๆ ซึ่งพลาดได้ถ้าตั้งชื่อไฟล์แบบอื่น
.env
.env.*
!.env.example
*.pem
*.key

# สคริปต์ไล่บั๊กชั่วคราว — ต้องวางที่รากโปรเจกต์ playwright ถึง resolve เจอ
# แต่ไม่ควรติดไปกับ repo (เผลอ commit ไปแล้วสองครั้ง 7 ก.ย. 69 ตอนใช้ git add -A)
t-*.mjs
t-*.ts
